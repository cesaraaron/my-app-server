type Client {
  id: ID! @unique
  name: String!
  lastName: String
  password: String!
  email: String @unique
  phoneNumber: String! @unique
  users: [User!]!
  products: [Product!]!
  sales: [Sale!]!
}

type User {
  id: ID! @unique
  name: String!
  client: Client!
  sales: [Sale!]!
}

type Product {
  id: ID! @unique
  name: String!
  price: Float!
  quantity: Int!
  client: Client!
}

type CartProduct {
  id: ID! @unique
  name: String!
  price: Float!
  quantitySold: Int!
  sale: Sale!
}

type Sale {
  id: ID! @unique
  products: [CartProduct!]!
  client: Client!
  soldBy: User!
}
