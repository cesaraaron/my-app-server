type Client {
  id: ID! @unique
  name: String!
  lastName: String
  password: String!
  email: String @unique
  phoneNumber: String! @unique
  users: [User!]!
  products: [Product!]!
  sales: [Sale!]!
}

type User {
  id: ID! @unique
  name: String!
  client: Client!
  sales: [Sale!]!
}

type Product {
  id: ID! @unique
  name: String!
  price: Float!
  isDeleted: Boolean @default(value: false)
  quantity: Int!
  client: Client!
}

type Sale {
  id: ID! @unique
  products: [CartProduct!]!
  isDeleted: Boolean @default(value: false)
  client: Client!
  soldBy: User!
}

type CartProduct {
  productId: ID!
  name: String!
  price: Float!
  quantitySold: Int!
}
