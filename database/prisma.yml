# The service name, this will be part of the endpoint of the Prisma API
service: my-app-server

# The deployment stage, this will also be part of the API endpoint
stage: dev

# Points to the file that holds your data model
datamodel: datamodel.graphql

# You can only access the API when providing JWTs that are signed with this secret
secret: mysecret123

endpoint: https://us1.prisma.sh/public-candlelion-41/my-app-server/dev

# hooks:
#   post-deploy:
#     - graphql get-schema --project database
#     - graphql prepare
