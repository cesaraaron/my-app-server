# import User, Product from "./generated/prisma.graphql"

type Query {
  users: [User!]!
  products: [Product!]!
}

type Mutation {
  login(phoneNumber: String!, password: String!): AuthPayload!
}

type AuthPayload {
  token: String!
}
